import { makeStyles } from '@material-ui/core/styles';

const useIdeListStyles = makeStyles({
  listItem: {
    display: 'block',
    padding: '0',
    fontWeight: 'bold',
    marginBottom: '32px',
  },
});

export default useIdeListStyles;
