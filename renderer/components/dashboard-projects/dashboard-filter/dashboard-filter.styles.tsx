import { createStyles, makeStyles, Theme } from '@material-ui/core/styles';

export const useDashboardFilterStyles = makeStyles((theme: Theme) =>
  createStyles({
    filterSelection: {
      width: '20em',
    },
  })
);
